[project]
name = "a-maze-ing"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mlx",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
]

[mypy-mlx]
ignore_missing_imports = true

[tool.uv.sources]
mlx = { path = "mlx-2.2-py3-none-any.whl" }
